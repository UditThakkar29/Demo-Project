<div class="d-flex">
  <%= render "navbar" %>

  <div class="ms-5 mt-2" style="width: 80%;">
    <h2 ><%= @ticket.name %> </h2>

    <p style="strong"><%= @ticket.summary %></p>

    <% if @ticket.priority== "High"%>
      <p class="text-danger"> <strong style="color: black;">Priority </strong>: High</p>
    <% elsif @ticket.priority== "Medium" %>
      <p class="text-warning-emphasis"><strong style="color: black;">Priority </strong>: Medium</p>
    <% else %>
    <p class="text-primary"><strong style="color: black;">Priority </strong>: Low</p>
    <% end %>

    <h3>Change Status</h3>
    <% if @ticket.to_do? %>
      <p>To-Do</p>
      <%= link_to "In Progress", doing_project_board_sprint_ticket_path(project_id: params[:project_id],id: @ticket),method: :patch, class:"btn btn-primary mb-4" %>
    <% elsif @ticket.progress? %>
      <p>In Progress</p>
      <%= link_to "QA", testing_project_board_sprint_ticket_path(project_id: params[:project_id],id: @ticket),method: :patch, class:"btn btn-primary mb-4" %>
    <% elsif @ticket.qa? %>
      <p>QA</p>
      <%= link_to "Back in progress", doing_project_board_sprint_ticket_path(project_id: params[:project_id],id: @ticket),method: :patch, class:"btn btn-primary mb-4" %>
      <%= link_to "Done", done_project_board_sprint_ticket_path(project_id: params[:project_id],id: @ticket),method: :patch, class:"btn btn-primary mb-4" %>
    <% else %>
      <h5>All done for this ticket</h5>
    <% end %>
    <br>

    <%= link_to "Assign Task to any user", edit_project_board_sprint_ticket_path(@ticket,project_slug: params[:project_slug],sprint_slug:params[:sprint_slug],board_slug:params[:board_slug]), class:"btn btn-primary" %>
  </div>

</div>




